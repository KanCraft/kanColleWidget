セットアップ
=========

```sh
npm install
sh cli/build
```

ブラウザの`chrome://extensions`のアドレスから、
`デベロッパーモード`を有効にして、
`パッケージ化されていない拡張機能を読み込む`を選択し、
上記のcliで生成された
`release/kanColleWidget`ディレクトリを選択。

テスト(ぜんぜん書いてない)
=========
```
npm run-script test
```
