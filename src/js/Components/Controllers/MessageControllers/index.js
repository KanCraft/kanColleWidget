import {
  GetConfig,
  SetConfig,
} from './Config';
import {
  OpenWindow,
} from './Window';
import { GetHistory } from './History';

const MessageControllers = {
  GetConfig,
  SetConfig,
  OpenWindow,
  GetHistory,
};

module.exports = MessageControllers;
